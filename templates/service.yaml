apiVersion: v1
kind: Service
metadata:
  name: {{ include "cpx-helm-vulnerable.fullname" . }}
  labels:
    {{- include "cpx-helm-vulnerable.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cpx-helm-vulnerable.selectorLabels" . | nindent 4 }}
